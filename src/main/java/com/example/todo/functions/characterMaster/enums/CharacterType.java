package com.example.todo.functions.characterMaster.enums;

/**
 * Enum representing the type of a character in the RPG game.
 * This enum is used to categorize characters into different types such as HERO, VILLAIN, and NPC.
 */
public enum CharacterType {
    HERO,
    VILLAIN,
    NPC
}
